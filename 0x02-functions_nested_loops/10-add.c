#include "holberton.h"
/**
* add - adds two integers and returns the result
* @a: an integer to add
* @b: an integer to add
* Return: integer value
*/
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
